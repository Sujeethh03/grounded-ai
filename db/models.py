"""SQLAlchemy models. See dossier Ledger Lens §2 "Database & graph schema" for
the full DDL (filings, doc_chunks, golden_qa) — port that SQL into models here
and generate the first Alembic migration from it as the M1 deliverable.
"""
