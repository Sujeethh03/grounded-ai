"""M5: LangGraph orchestrator — decides RAG vs. graph vs. both, fans out.

Not implemented yet. Uses the shared agent harness described in the Portfolio
Dossier's Sentinel §3 (ToolCall protocol, structured-output validation,
per-agent step/token budget) rather than reimplementing retry/timeout logic here.
"""
